apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f ../docker-compose.yml
    kompose.version: 1.28.0 (c4137012e)
  creationTimestamp: null
  labels:
    io.kompose.service: payment-mysql
  name: payment-mysql
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: payment-mysql
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f ../docker-compose.yml
        kompose.version: 1.28.0 (c4137012e)
      creationTimestamp: null
      labels:
        io.kompose.network/backend-gobbler-net: "true"
        io.kompose.service: payment-mysql
    spec:
      containers:
        - env:
            - name: MYSQL_ROOT_PASSWORD
              # value: ${DATABASE_PASSWORD}
              valueFrom:
                secretKeyRef:
                  name: gobbler
                  key: DATABASE_PASSWORD
          image: asia-southeast1-docker.pkg.dev/${GCP_PROJECT_ID}/gobbler/payment-mysql:latest
          livenessProbe:
            exec:
              command:
                - mysqladmin
                - ping
                - -h
                - localhost
                - -u
                - ${DATABASE_USER}
                - -p${DATABASE_PASSWORD}
            failureThreshold: 5
            periodSeconds: 10
            timeoutSeconds: 5
          name: payment-mysql
          ports:
            - containerPort: 3306
          resources: {}
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: payment-mysql-data
            - mountPath: /var/log/mysql
              name: payment-mysql-logs
      restartPolicy: Always
      volumes:
        - name: payment-mysql-data
          persistentVolumeClaim:
            claimName: payment-mysql-data
        - name: payment-mysql-logs
          persistentVolumeClaim:
            claimName: payment-mysql-logs
status: {}
